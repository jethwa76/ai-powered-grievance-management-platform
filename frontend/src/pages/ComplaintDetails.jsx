import { Link, useParams } from 'react-router-dom'; import { useQuery } from '@tanstack/react-query'; import { ArrowLeft, Clock3, MapPin, MessageSquare, Sparkles } from 'lucide-react'; import { io } from 'socket.io-client'; import { useEffect, useState } from 'react'; import api from '../lib/api'; import { Badge, Button, Card, Loading, Timeline } from '../components/Ui'; import { formatDate, formatStatus } from '../lib/format';
export default function ComplaintDetails(){const {id}=useParams(); const {data,isLoading}=useQuery({queryKey:['complaint',id],queryFn:async()=> (await api.get(`/complaints/${id}`)).data.data}); const [live,setLive]=useState(false); useEffect(()=>{if(!id)return; const socket=io(import.meta.env.VITE_SOCKET_URL||'http://localhost:4000'); socket.emit('complaint:subscribe',id); socket.on('complaint:status',()=>setLive(true)); return()=>socket.disconnect()},[id]); if(isLoading)return <Loading/>; const complaint=data?.complaint; if(!complaint)return <div className="empty-state">Complaint not found.</div>; return <><Link to="/complaints" className="back-link content-back"><ArrowLeft size={15}/> All complaints</Link><div className="details-head"><div><div className="eyebrow">{complaint.ticketId}</div><h1>{complaint.title}</h1><div className="detail-sub"><MapPin size={14}/> {complaint.location?.address} <span>·</span> Submitted {formatDate(complaint.createdAt)}</div></div><Badge>{complaint.status}</Badge></div>{live&&<div className="live-alert"><span className="live"><i/> LIVE</span> This ticket just received an update. Refresh to see the latest details.</div>}<div className="details-grid"><Card><div className="card-header"><div><h3>Progress timeline</h3><p>Every step, in one place.</p></div><Clock3 size={19} className="muted-icon"/></div><Timeline items={data.timeline}/></Card><div className="details-side"><Card><div className="card-header"><h3>Ticket details</h3></div><dl className="details-list"><div><dt>Department</dt><dd>{complaint.department?.name||'Being routed'}</dd></div><div><dt>Priority</dt><dd><Badge tone={complaint.priority==='critical'?'coral':undefined}>{complaint.priority}</Badge></dd></div><div><dt>Category</dt><dd>{complaint.category||'Other'}</dd></div><div><dt>Last updated</dt><dd>{formatDate(complaint.updatedAt)}</dd></div></dl></Card><Card><div className="ai-note large"><Sparkles size={16}/><div><strong>Smart routing insight</strong><p>This ticket was classified and routed with an AI confidence score. A human reviews any uncertain decision.</p></div></div></Card></div></div>{complaint.status==='resolved'&&<Card className="feedback-card"><MessageSquare size={19}/><div><h3>How did we do?</h3><p>Let us know if the resolution worked for you.</p></div><Button variant="secondary">Give feedback</Button></Card>}</>}
