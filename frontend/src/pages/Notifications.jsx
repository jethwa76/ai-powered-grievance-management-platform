import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'; import { Bell, CheckCheck } from 'lucide-react'; import api from '../lib/api'; import { Card, EmptyState, Loading, PageTitle } from '../components/Ui'; import { formatDate } from '../lib/format';
export default function Notifications(){const client=useQueryClient(); const {data,isLoading}=useQuery({queryKey:['notifications'],queryFn:async()=> (await api.get('/notifications')).data.data.notifications}); const mark=useMutation({mutationFn:(id)=>api.patch(`/notifications/${id}/read`),onSuccess:()=>client.invalidateQueries({queryKey:['notifications']})}); if(isLoading)return <Loading/>; const list=data||[]; return <><PageTitle eyebrow="NOTIFICATIONS" title="Stay in the know" description="Updates about your complaints and the teams working on them." action={<button className="text-link" onClick={()=>list.filter((item)=>!item.readAt).forEach((item)=>mark.mutate(item._id))}><CheckCheck size={16}/> Mark all as read</button>}/><Card className="notification-list">{list.length?list.map((item)=><button className={item.readAt?'notification-row read':'notification-row'} key={item._id} onClick={()=>!item.readAt&&mark.mutate(item._id)}><span className="notification-icon"><Bell size={16}/></span><span className="notification-copy"><strong>{item.title}</strong><span>{item.message}</span><small>{formatDate(item.createdAt)} {item.complaint?.ticketId&&` · ${item.complaint.ticketId}`}</small></span>{!item.readAt&&<i className="unread-dot"/>}</button>):<EmptyState title="You’re all caught up" description="New updates will appear here."/>}</Card></>}
