import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { ArrowLeft, ArrowRight, Eye, EyeOff, ShieldCheck } from 'lucide-react'; import { useAuth } from '../context/AuthContext';
export default function Auth({mode}){const login=mode==='login'; const {login:signIn,register}=useAuth(); const navigate=useNavigate(); const [show,setShow]=useState(false); const [form,setForm]=useState(login?{email:'',password:''}:{name:'',email:'',password:'',preferredLanguage:'en'}); const [error,setError]=useState(''); const [busy,setBusy]=useState(false); const update=(e)=>setForm({...form,[e.target.name]:e.target.value}); const submit=async(e)=>{e.preventDefault(); setBusy(true); setError(''); try { if(login) await signIn(form); else await register(form); navigate('/dashboard'); } catch(error){setError(error.response?.data?.error?.message||'Unable to complete that request.');} finally{setBusy(false)} }; return <div className="auth-page"><div className="auth-aside"><Link to="/" className="brand"><span className="brand-mark">✦</span><span>Civic<span>Flow</span></span></Link><div><div className="eyebrow light">CIVIC PARTICIPATION, REIMAGINED</div><h1>Good governance starts with being <em>heard.</em></h1><p>Track your concerns from the first report to the final resolution — with clarity at every step.</p></div><div className="aside-footer"><ShieldCheck size={16}/> Your information is protected with enterprise-grade security.</div></div><div className="auth-main"><Link to="/" className="back-link"><ArrowLeft size={16}/> Back to home</Link><div className="auth-form"><div className="eyebrow">{login?'WELCOME BACK':'JOIN CIVICFLOW'}</div><h2>{login?'Sign in to your workspace':'Create your citizen account'}</h2><p className="muted">{login?'Pick up where you left off.':'It takes less than a minute to get started.'}</p><form onSubmit={submit}>{!login&&<label>Full name<input name="name" value={form.name} onChange={update} placeholder="Your name" required/></label>}<label>Email address<input name="email" type="email" value={form.email} onChange={update} placeholder="you@example.com" required/></label><label>Password<div className="password-input"><input name="password" type={show?'text':'password'} value={form.password} onChange={update} placeholder={login?'Your password':'At least 10 characters'} required minLength={login?1:10}/><button type="button" className="password-toggle" onClick={()=>setShow(!show)}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label>{!login&&<label>Preferred language<select name="preferredLanguage" value={form.preferredLanguage} onChange={update}><option value="en">English</option><option value="hi">हिन्दी</option><option value="mr">मराठी</option></select></label>}{error&&<div className="form-error">{error}</div>}<button className="button primary full" disabled={busy}>{busy?'Please wait…':login?'Sign in':'Create account'} {!busy&&<ArrowRight size={17}/>}</button></form><div className="auth-switch">{login?<>New to CivicFlow? <Link to="/signup">Create an account</Link></>:<>Already have an account? <Link to="/login">Sign in</Link></>}</div></div></div></div>}
