import { useQuery } from '@tanstack/react-query'; import { BarChart3, ClipboardCheck, Clock3, FileWarning, Users, Zap } from 'lucide-react'; import { Bar, BarChart, CartesianGrid, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'; import api from '../lib/api'; import { Card, Loading, PageTitle, StatCard } from '../components/Ui';
const colors=['#0b7285','#ee7654','#f4b942','#3c8d71'];
export default function Admin(){const {data,isLoading}=useQuery({queryKey:['analytics'],queryFn:async()=> (await api.get('/admin/analytics')).data.data}); if(isLoading)return <Loading/>; const status=data?.statuses||[]; const priorities=Object.entries(data?.byPriority||{}).map(([name,count])=>({name,count})); const total=data?.totalComplaints||0; return <><PageTitle eyebrow="OPERATIONS CENTER" title="The city, at a glance." description="A live view of grievance health, service performance, and AI-assisted decisions." action={<button className="button secondary"><BarChart3 size={16}/> Export report</button>}/><div className="stat-grid admin-stats"><StatCard label="Total complaints" value={total} change="Across all departments" icon={FileWarning}/><StatCard label="Pending review" value={data?.pendingReview||0} change="AI confidence needs attention" icon={ClipboardCheck} accent="amber"/><StatCard label="Avg. resolution" value={`${data?.averageResolutionHours||'0.0'}h`} change="From submission to resolution" icon={Clock3} accent="mint"/><StatCard label="AI confidence" value={`${Math.round((data?.confidence?.average||0)*100)}%`} change={`${data?.confidence?.low||0} low-confidence predictions`} icon={Zap} accent="coral"/></div><div className="charts-grid"><Card><div className="card-header"><div><h3>Complaint flow</h3><p>Current ticket status distribution</p></div></div><div className="chart-wrap"><ResponsiveContainer width="100%" height="100%"><BarChart data={status.map((item)=>({name:item._id?.replaceAll('_',' '),count:item.count}))}><CartesianGrid vertical={false} stroke="var(--line)"/><XAxis dataKey="name" tickLine={false} axisLine={false} tick={{fill:'var(--muted)',fontSize:11}}/><YAxis tickLine={false} axisLine={false} tick={{fill:'var(--muted)',fontSize:11}}/><Tooltip/><Bar dataKey="count" fill="#0b7285" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></div></Card><Card><div className="card-header"><div><h3>Priority mix</h3><p>What needs attention first</p></div></div><div className="chart-wrap pie"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={priorities} dataKey="count" nameKey="name" innerRadius={57} outerRadius={84} paddingAngle={3}>{priorities.map((_entry,index)=><Cell key={index} fill={colors[index%colors.length]}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></div><div className="legend">{priorities.map((item,index)=><span key={item.name}><i style={{background:colors[index%colors.length]}}/>{item.name} <b>{item.count}</b></span>)}</div></Card></div><Card className="performance-card"><div className="card-header"><div><h3>Performance snapshot</h3><p>Operational signals for department leads</p></div><Users size={19} className="muted-icon"/></div><div className="performance-grid"><div><strong>94.2%</strong><span>Citizen satisfaction</span><small className="positive">↑ 4.8% this month</small></div><div><strong>1.8d</strong><span>First response time</span><small className="positive">↓ 0.4d this month</small></div><div><strong>12.6%</strong><span>Duplicate reports</span><small>218 linked tickets</small></div><div><strong>87%</strong><span>Within SLA</span><small className="positive">↑ 7% this month</small></div></div></Card></>}
