import { useQuery } from '@tanstack/react-query'; import { Check, ChevronRight, CircleAlert, Sparkles } from 'lucide-react'; import { Link } from 'react-router-dom'; import api from '../lib/api'; import { Badge, Card, EmptyState, Loading, PageTitle } from '../components/Ui'; import { formatDate } from '../lib/format'; import { useT } from '../context/LanguageContext';
export default function Review(){const t=useT(); const {data,isLoading}=useQuery({queryKey:['review-queue'],queryFn:async()=>(await api.get('/review-queue')).data.data.items}); if(isLoading)return <Loading/>; const items=data||[]; return <><PageTitle eyebrow={t('review_eyebrow')} title={t('review_title')} description={t('review_subtitle')} action={<div className="queue-count"><CircleAlert size={16}/>{items.length} {t('review_pending')}</div>}/><div className="review-list">{items.length?items.map((item)=><Card className="review-row" key={item._id}><div className="review-icon"><Sparkles size={19}/></div><div className="review-copy"><div className="review-row-head"><span className="eyebrow">{item.complaint?.ticketId}</span><small>{formatDate(item.createdAt)}</small></div><h3>{item.complaint?.title}</h3><p>{item.complaint?.description}</p><div className="review-meta"><Badge tone="amber">{t('review_badge')}</Badge><span>{item.reason}</span></div></div><Link to={`/complaints/${item.complaint?._id}`} className="button secondary">{t('review_btn')} <ChevronRight size={15}/></Link></Card>):<EmptyState title={t('review_empty_title')} description={t('review_empty_sub')}/>}</div></>}
