import { Link } from 'react-router-dom'; import { useQuery } from '@tanstack/react-query'; import { ArrowUpRight, Clock3, FileText, Plus, Sparkles, TrendingUp } from 'lucide-react'; import { useAuth } from '../context/AuthContext'; import api from '../lib/api'; import { formatDate, formatStatus } from '../lib/format'; import { Badge, Button, Card, EmptyState, Loading, PageTitle, StatCard } from '../components/Ui'; import { useT } from '../context/LanguageContext';
export default function Dashboard(){const {user}=useAuth(); const t=useT(); const {data,isLoading}=useQuery({queryKey:['complaints'],queryFn:async()=>(await api.get('/complaints?limit=5')).data.data}); if(isLoading)return <Loading/>; const complaints=data?.complaints||[]; const open=complaints.filter((item)=>!['resolved','closed'].includes(item.status)).length; return <><PageTitle eyebrow={t('dash_eyebrow')} title={`${t('dash_greeting')}, ${user?.name?.split(' ')[0]||'there'}.`} description={t('dash_subtitle')} action={<Link to="/complaints/new" className="button primary"><Plus size={17}/> {t('dash_lodge_btn')}</Link>}/><div className="stat-grid"><StatCard label={t('dash_stat_active')} value={open} change={t('dash_stat_active_sub')} icon={FileText}/><StatCard label={t('dash_stat_resolved')} value={complaints.filter((item)=>item.status==='resolved'||item.status==='closed').length} change={t('dash_stat_resolved_sub')} icon={TrendingUp} accent="mint"/><StatCard label={t('dash_stat_avg')} value="2.4d" change={t('dash_stat_avg_sub')} icon={Clock3} accent="coral"/></div><div className="dashboard-grid"><Card className="recent-card"><div className="card-header"><div><h3>{t('dash_recent_title')}</h3><p>{t('dash_recent_sub')}</p></div><Link to="/complaints" className="text-link">{t('dash_view_all')} <ArrowUpRight size={15}/></Link></div>{complaints.length?<div className="complaint-list">{complaints.map((item)=><Link to={`/complaints/${item._id}`} className="complaint-row" key={item._id}><div className="complaint-symbol">{item.title?.[0]}</div><div className="complaint-main"><strong>{item.title}</strong><span>{item.ticketId} · {item.department?.name||t('dash_routing')}</span></div><div className="complaint-meta"><Badge>{item.status}</Badge><small>{formatDate(item.createdAt)}</small></div></Link>)}</div>:<EmptyState title={t('dash_empty_title')} description={t('dash_empty_sub')}/>}</Card><Card className="side-card"><div className="ai-banner"><div className="sparkle"><Sparkles size={17}/></div><div><strong>{t('dash_ai_title')}</strong><p>{t('dash_ai_body')}</p></div></div><div className="side-tip"><span>{t('dash_tip_label')}</span><p>{t('dash_tip_body')}</p><Link to="/complaints/new">{t('dash_tip_link')} <ArrowUpRight size={14}/></Link></div></Card></div></>}
